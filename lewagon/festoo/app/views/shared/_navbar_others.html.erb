<div class="navbar navbar-expand-sm navbar-light navbar-lewagon-white fixed-top">
  <%= link_to root_path, class: "navbar-brand" do %>
    <%= image_tag "FestooLogo500.png" %>
  <% end %>

  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>


  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <% if user_signed_in? %>
        <li class="nav-item active">
          <%= link_to 'Cadastrar produto', new_product_path, class: "nav-link"  %>
        </li>

        <li class="nav-item dropdown">
          <% if user_signed_in? %>
            <!-- ADAPTAR IF-STATEMENT PARA STRING DA FOTO DO USUARIO -->
            <!-- Cortar e colar a linha de baixo no else -->
            <%= image_tag "no-profile.png", class: 'avatar dropdown-toggle', id: "navbarDropdown", data: { toggle: "dropdown" }, 'aria-haspopup': true, 'aria-expanded': false %>
          <% else %>
            I<!-- NSIRA AQUI A IMAGE-TAG NO-PROFILE -->
            <%= image_tag "no-profile.png", class: 'avatar dropdown-toggle', id: "navbarDropdown", data: { toggle: "dropdown" }, 'aria-haspopup': true, 'aria-expanded': false %>
          <% end %>

          <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
            <%= link_to "Meu Perfil", edit_user_registration_path, class: "dropdown-item" %>
            <%= link_to "Meus Produtos", meus_produtos_path, class: "dropdown-item" %>
            <%= link_to "Sair", destroy_user_session_path, method: :delete, class: "dropdown-item" %>
          </div>
        </li>
      <% else %>
        <li class="nav-item active">
          <%= link_to "Login", new_user_session_path, class: "nav-link" %>
        </li>
        <li class="nav-item active">
          <%= link_to "Sign-Up", "#", class: "nav-link" %>
        </li>
      <% end %>
    </ul>
  </div>
</div>
